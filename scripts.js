/*
 * Escribe una función que recibe dos parámetros:
 * 1) Un array con nombres de paises
 * 2) El nombre de un país
 *
 * La función debe devolver true si el país se encuentra en el array
 * de lo contrario devuelve false.
 *
 * No puedes utilizar las funciones nativas de JavaScript.
 *
 * Ejemplo:
 * const countries = ['Canada', 'Mexico', 'Brazil'];
 * findCountry(countries, 'Mexico'); // Should return true
 * findCountry(countries, 'USA'); // Should return false
 */
